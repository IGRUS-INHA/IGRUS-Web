name: Frontend CI Pipeline

on:
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  check-changes:
    runs-on: ubuntu-latest
    outputs:
      frontend: ${{ steps.filter.outputs.frontend }}
    steps:
      - uses: actions/github-script@v7
        id: filter
        with:
          script: |
            const pr = context.payload.pull_request;
            const files = await github.paginate(github.rest.pulls.listFiles, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pr.number,
              per_page: 100
            });
            const frontendChanged = files.some(f =>
              (f.filename.startsWith('frontend/') && !f.filename.startsWith('frontend/docs/')) ||
               f.filename === '.github/workflows/frontend-ci.yaml'
            );
            core.setOutput('frontend', frontendChanged ? 'true' : 'false');

  build-and-lint:
    needs: check-changes
    if: needs.check-changes.outputs.frontend == 'true'
    runs-on: ubuntu-latest
    environment: staging
    env:
      VITE_API_URL: ${{ vars.VITE_API_URL }}
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Type check
        run: npx tsc --noEmit

      - name: Format check
        run: npx prettier --check .

      - name: Build
        run: pnpm build
        env:
          VITE_API_URL: ${{ env.VITE_API_URL }}
          FEATURE_COMMUNITY: 'false'
          FEATURE_SEARCH: 'false'
          FEATURE_PROFILE_EDIT: 'false'
